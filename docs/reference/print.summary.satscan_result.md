# Print Method for Summary of SaTScan Results

Print Method for Summary of SaTScan Results

## Usage

``` r
# S3 method for class 'summary.satscan_result'
print(x, raw = FALSE, ...)
```

## Arguments

- x:

  A `summary.satscan_result` object.

- raw:

  Logical. If `TRUE` and available, print the raw SaTScan text output.

- ...:

  Additional arguments (currently unused).

## Value

Invisibly returns `x`.
