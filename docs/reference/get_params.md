# Extract Analysis Parameters from Result

Retrieves stored analysis parameters from a satscan_result object.

## Usage

``` r
get_params(x)
```

## Arguments

- x:

  A `satscan_result` object.

## Value

List of parameters, or NULL if not stored.
