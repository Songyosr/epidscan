# Format Time for SaTScan

Format Time for SaTScan

## Usage

``` r
format_satscan_time(x, time_precision)
```

## Arguments

- x:

  Vector of times (Date, POSIXt, numeric, character)

- time_precision:

  "day", "month", "year", or "generic"

## Value

Character vector formatted for SaTScan
