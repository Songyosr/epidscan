# Load Default PRM Parameters

Loads internal default parameters as a prm_list.

## Usage

``` r
prm_defaults(version = NULL)
```

## Arguments

- version:

  Optional SaTScan version (e.g., "10.3"). If NULL, uses latest (10.3).

## Value

A `prm_list` parsed from bundled template files.
