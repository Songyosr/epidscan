% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss_tbl.R
\name{ss_grd}
\alias{ss_grd}
\title{Coerce Data to SaTScan Grid Table}
\usage{
ss_grd(
  data,
  coord1,
  coord2,
  z = NULL,
  earliest_start = NULL,
  latest_start = NULL,
  earliest_end = NULL,
  latest_end = NULL,
  grid_variant = "basic"
)
}
\arguments{
\item{data}{A data frame containing grid locations.}

\item{coord1}{Column name for the first coordinate.}

\item{coord2}{Column name for the second coordinate.}

\item{z}{Optional column name for Z-coordinate.}

\item{earliest_start}{Optional column for earliest start time constraint.}

\item{latest_start}{Optional column for latest start time constraint.}

\item{earliest_end}{Optional column for earliest end time constraint.}

\item{latest_end}{Optional column for latest end time constraint.}

\item{grid_variant}{Variant of grid file ("basic", etc.).}
}
\value{
An \code{ss_tbl} object of type "grd".
}
\description{
Creates an \code{ss_tbl} object of type \code{"grd"} (SaTScan Grid File).
A grid file specifies the set of candidate centroids at which SaTScan
evaluates potential clusters. When a grid is supplied, SaTScan scans only
at these grid points (rather than at every observed location), which can
reduce computation and enforce a user-defined scanning lattice.

This constructor records the coordinate columns (and optional constraints)
without modifying the input data. Coordinate ordering rules match \code{ss_geo}
and are applied by the writer when producing the SaTScan ASCII file.
}
\section{SaTScan File Specification}{

The Grid File has the same structure as the Coordinates File:
\code{<LocationID> <Latitude/Y> <Longitude/X>}

\itemize{
\item \strong{LocationID}: Unique identifier for the grid point.
\item \strong{Coordinates}: Same rules as \code{ss_geo} (Lat/Long order vs X/Y order).
}
}

