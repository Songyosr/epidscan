% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_ctl.R
\name{prep_ctl}
\alias{prep_ctl}
\title{Prepare SaTScan Control File (Bernoulli)}
\usage{
prep_ctl(
  df,
  loc_id,
  time = NULL,
  cases = NULL,
  covars = NULL,
  style = c("casewise", "aggregated"),
  time_precision = c("day", "month", "year", "generic")
)
}
\arguments{
\item{df}{Data frame containing control data.}

\item{loc_id}{Column name for location ID (unquoted).}

\item{time}{Column name for time/date (unquoted, optional). Required for Space-Time analyses.}

\item{cases}{Column name for control counts (unquoted, optional). If NULL, assumes 1 control per row.}

\item{covars}{Character vector of covariate column names.}

\item{style}{Input style: "casewise" or "aggregated".}

\item{time_precision}{Time resolution: "day", "month", "year", or "generic".}
}
\value{
A \code{satscan_table} object of kind "ctl".
}
\description{
Prepares a control file for Bernoulli models.
Similar to \code{prep_cas}, but for controls (non-cases).
}
\section{SaTScan File Specification}{

The Control File has the following structure:
\code{<LocationID> <NoControls> <Date> <Covariate1> ...}

\itemize{
  \item \strong{LocationID}: Identifier matching Case/Geo files.
  \item \strong{NoControls}: Number of controls.
  \item \strong{Date}: Required for Space-Time Bernoulli. Same format rules as Case file.
  \item \strong{Covariates}: Optional.
}
}

