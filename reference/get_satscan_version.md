# Get SaTScan Version

Attempts to determine the version of the configured SaTScan executable.

## Usage

``` r
get_satscan_version()
```

## Value

Character string containing the version (e.g. "10.2.5") or NULL if
unable to determine.
