# Create Tidy Data Frame

Converts to tibble if available, otherwise returns data.frame.

## Usage

``` r
make_tidy_df(df)
```

## Arguments

- df:

  A data frame

## Value

Tibble if available, otherwise data.frame
