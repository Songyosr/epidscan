# Detect Coordinate Columns

Detect Coordinate Columns

## Usage

``` r
detect_coordinates(df, meta = NULL)
```

## Arguments

- df:

  Data frame with location coordinates

- meta:

  Optional summary_info list with 'coord_cols' and 'coord_type_code'

## Value

List with lat_col, lon_col, and type
