# Add Cluster Circles to Map

Add Cluster Circles to Map

## Usage

``` r
add_cluster_circles(
  map,
  cluster_data,
  color_palette,
  opacity,
  use_radius = TRUE,
  is_simple = FALSE,
  ...
)
```
